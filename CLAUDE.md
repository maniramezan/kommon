# kommon Agent Entry Point

Use `AGENTS.md` as the source of truth for agent instructions in this repo.

When repo guidance appears stale, prefer the checked-in Gradle/module configuration, `gradle/libs.versions.toml`, and `settings.gradle.kts` over older references in this file.
